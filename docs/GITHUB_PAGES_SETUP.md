# Activation initiale de GitHub Pages

1. Ouvrir **GitHub → Repository → Settings → Pages**.
2. Dans **Build and deployment → Source**, choisir **GitHub Actions**.
3. Ouvrir **Actions → Deploy Web Preview → Run workflow → Run workflow**.

